import type { ReactNode } from 'react';

export const metadata = { title: 'Contract Functions' };

export default function ContractFunctionsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
